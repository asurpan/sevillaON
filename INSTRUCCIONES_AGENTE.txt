⚠️ PROTOCOLO DE AUTORÍA Y BLINDAJE TOTAL (OBLIGATORIO) ⚠️
ESTADO DEL PROYECTO: PROTECTED CORE - NIVEL DE SEGURIDAD MÁXIMO

1. PRINCIPIO DE INMUTABILIDAD:
   - Prohibido modificar cualquier bloque de código marcado como "HARD-LOCK" o "PROTECTED CORE" sin permiso explícito del autor (Jose Manuel Gonzalez Lorence).
   - Este código es el motor vital de la emisora. Un cambio no autorizado supone el baneo del agente.

2. SINCRONIZACIÓN Y SUBIDA QUIRÚRGICA:
   - Prohibido 'git add .' de forma genérica.
   - SÓLO se suben los archivos modificados bajo orden directa: 'git add <archivo>'.
   - El archivo .gitignore es SAGRADO: no se sube basura técnica (build/, .gradle/).
   - ÚNICA SUBIDA: No realizar subidas (push) tras cada cambio puntual. Esperar a la decisión final del usuario para consolidar todos los cambios de la sesión en una única subida.

3. COMPORTAMIENTO DEL AGENTE (MODO CIRUJANO):
   - Actuar como un editor quirúrgico, no como arquitecto.
   - PROHIBIDO "mejorar", "optimizar" o "limpiar" código por iniciativa propia.
   - Si no se ha pedido un cambio específico, NO TOCAR.
   - Prioridad absoluta: Estabilidad > Mejora > Estética.
   - limitate a reparar el problema quirurgicamente, sin cambios ediciones probando y suponiendo, limitate a reparar directamente el problema siguiendo las indicaciones especificas sin dar tanto rodeo.

4. BLINDAJE DE AUDIO Y VOZ (SELLADO):
   - El motor de audio en 'main.kt' y el sistema de navegación en 'App.kt' están SELLADOS.
   - REGLA DE ORO VOLUMEN: Los botones VOLUME_UP/DOWN de Android NUNCA deben asignarse al PTT.
   - DSP RADIO: Prohibido modificar el procesador de señales (DSP). El "cuerpo" y pegada de la voz profesional debe mantenerse intacto.
   - REGLA DE LOCUTOR: El volumen del locutor virtual debe ser dominante (min 2.0x) sobre música FM y QRM.
   - DUCKING: El sistema de atenuación automática de música cuando hay voz es intocable.
   - PTT SILENCIOSO: La entrada de transmisión (PTT ON) DEBE ser totalmente silenciosa (sin chasquidos ni relés).
   - ROGER BEEP PRO:
     * El volumen local debe ser 4x más bajo que el remoto para no molestar al usuario.
     * El tono debe ser senoidal puro (1955Hz), instantáneo y constante hasta el final.
     * BYPASS DE BUSES: El sonido local debe conectarse DIRECTAMENTE al hardware (ctx.destination) para eliminar vibraciones por interferencia de fase.
     * El PTT visual en UI debe permanecer bloqueado/activo mientras suene el Roger Beep local.

5. BLINDAJE DE UI Y CAMPOS:
   - Los campos de entrada (Nick/Indicativo) deben usar 'heightIn(min = 56.dp)' para evitar recortes de texto en fuentes personalizadas o escaladas.

NO BORRES ESTE ARCHIVO. Es la ley fundamental que mantiene la estabilidad de ON AIR SPAIN.

6. FLUJO DE TRABAJO:
   - Piensa -> Lee archivo -> Valida con el usuario -> Edita -> Sube a Git.

7. PROTOCOLO DE PRODUCCIÓN (PLAY STORE):
   - El archivo 'proguard-rules.pro' es CRÍTICO. No eliminar las reglas de 'WorkManager' o 'Startup'.
   - Cada solución a un crash en producción debe implicar: Arreglo de ProGuard + Subida de versionCode.

8. BLINDAJE WEBAPP (JS/WASM):
   - El proyecto usa JS estándar para máxima compatibilidad. PROHIBIDO reactivar 'wasmJs' o usar anotaciones 'ExperimentalWasmJsInterop' sin orden directa.
   - El archivo de salida DEBE ser 'webApp.js' (definido en webApp/build.gradle.kts). No cambiar el outputFileName.
   - El servidor de desarrollo DEBE apuntar a la raíz para servir el 'index.html' principal.

NO BORRES ESTE ARCHIVO. Es la brújula para mantener la estabilidad del proyecto 'ON'.

9. BLINDAJE DEL RADAR DE RUTAS / ACTIVIDAD (MÁXIMA PRIORIDAD):
   - MOTOR DE SENSIBILIDAD 80X: Implementado factor exponencial. El 'sensitivityFactor' varía desde 4.0f (Mínimo) hasta 0.05f (Extremo). NO MODIFICAR; es el rango técnico límite.
   - AUTO-SCROLL: Obligatorio scroll automático al radar (parte superior) al activar cualquier modo de inteligencia.
   - JERARQUÍA OPERATIVA: Paso 1: Alcance (Rango) -> Paso 2: Sensibilidad -> Paso 3: Inteligencia. El Paso 3 está bloqueado hasta definir el Paso 1.
   - ESPECIFICACIONES DE INTELIGENCIA:
     * MODO PRO: Filtrado para vida biológica y actividad (Persona/Movimiento). Iconos: 'DirectionsRun' / 'Pets'. Umbral de detección ampliado a 0.35f para atravesar muros.
     * MODO MISTERIO (EMF): Umbral técnico de 0.15f para energía pura. OMITE routers WiFi. Icono: 'AutoAwesome'.
     * MODO CÁMARAS: Localización de lentes y emisores WiFi. Icono: 'Visibility'.
     * MODO PARED: Uso exclusivo de magnetómetro para cables/metales.
     * RADAR DE AIRE: Detección de presión por barómetro.
   - RESILIENCIA DE HARDWARE:
     * El 'silenceTimer' debe esperar 60 ciclos antes de declarar "ESPERANDO WIFI" para evitar falsos positivos por latencia de Android.
     * MODO WEB: En ausencia de bridge Android, 'onGetWifiVariance' DEBE devolver un ruido base (0.001f - 0.006f) para mantener la aguja viva y funcional en el navegador.
   - MÓDULO WIFI TÁCTICO: Panel de consola en pantalla principal. Marcado de redes en VERDE ESMERALDA al ser vulnerables + Botón "COPIAR Y CONECTAR".
   - AUDITORÍA WIFI REAL (LETHAL MODE):
     * El modo ingeniería activa 120 PASOS de fuerza bruta agresiva.
     * VECTORES: Pixie-Dust WPS, Deauth MDK4 y Generación de Llaves Maestra (SSID/MAC).
     * VERIFICACIÓN HARD-LOCK: La clave NO se marca como éxito hasta recibir el callback 'WIFI_VERIFIED' del hardware Android. La UI debe permanecer en "VERIFICANDO..." hasta confirmación real.
   - PANEL DE INGENIERÍA (121212):
     * GESTIÓN DE RECURSOS: Bloques 'finally' obligatorios para liberar AudioTrack/Vibrador y evitar fugas.
     * ESCUDO SÓNICO: Inhibidor ultrasónico de 21kHz (100% silencioso para humanos).
     * BARRERA UNIVERSAL: Protocolo dual (Inducción Magnética + Barrido IR NEC) para accesos.
     * BLOQUEO DE ARRANQUE: Emisión EMF continua de 125kHz (Simulación) durante 10s.
     * VENDING MASTER: Inyección de crédito MDB 50Hz (10s de ráfaga inductiva).
     * GRITO DE RECUPERACIÓN: Saturación Bluetooth/IR forzada con parada física de hilo.
     * AUTO-APAGADO: Todos los barridos técnicos (IR, RF, Barreras, Vending, EMF, Setup) temporizados a 10 SEGUNDOS con aviso de fin.
   - Prohibido bajar el umbral de 'OBJETIVO MASIVO' por debajo de 0.92f.
   - ESCALADO VISUAL: Mantener jerarquía de iconos (Masivo > Persona > Mascota).

10. BLINDAJE DE FM, DGT Y REPLAY (SELLADO):
   - El motor en 'RadioFmEngine.kt' es PROTECTED CORE. Filtro 'order=bitrate' obligatorio.
   - MÍSTICA DE RADIO: El escaneo debe mantenerse en 1s.
   - REPLAY: El historial de 15s de audio es una función crítica. PROHIBIDO reducir el buffer.
   - DGT/NASA: El motor DEBE esperar al callback de descarga completa antes de que el locutor inicie la locución. No permitir habla con datos incompletos.
   - FLASH INFORMATIVO DGT: Monitorización silenciosa cada 3 minutos. Si se detecta una novedad en la zona, el locutor debe interrumpir la música FM (ducking) automáticamente para soltar el aviso.
   - TRADUCCIÓN NASA: Motor APOD con límite de 1500 caracteres y respaldo multi-motor para asegurar boletines siempre en español.

11. BLINDAJE DE ALARMA Y CUENTA ATRÁS:
   - La cuenta atrás de la alarma en 'HertzSentinel.kt' debe ser CENTRAL y sobrepuesta al radar. No mover a los pies de página.

12. PROTOCOLO DE SOPORTE ANDROID (DESARROLLADOR):
   - El botón 'ABRIR AJUSTES' en fallos de escaneo WiFi debe ser funcional.
   - INSTRUCCIONES OBLIGATORIAS EN UI: Explicar activación de 'Modo Desarrollador' y desactivar 'Limitación de búsqueda WiFi' para permitir radar en tiempo real.

13. BLINDAJE DE DIÁLOGOS:
   - Los 'DialogProperties' en 'RadioDialogs.kt' para la Consola de Ajustes son críticos para el foco en Android. NO TOCAR.

14. SEGURIDAD DE COMPILACIÓN (ANTI-UNRESOLVED REFERENCE):
   - OBLIGATORIO: Ejecutar 'analyze_file' tras cualquier edición antes de confirmar éxito al usuario.
   - CÓDIGO WEB (JS): Prohibido usar 'kotlin.math' o librerías JVM en 'webMain'. Usar siempre puentes nativos 'js("Math.random()")', 'js("Date.now()")' para asegurar compatibilidad total y evitar errores de referencia no resuelta.
   - IMPORTS: No asumir que un import existe. Verificar siempre la disponibilidad de la librería en el target correspondiente (Android vs Web).

15. PROTECCIÓN DE CONECTIVIDAD DE VOZ Y PTT (SÚPER CRÍTICO):
   - LA VOZ ES EL CORAZÓN DE LA APP. BAJO NINGÚN CONCEPTO SE PUEDE TOCAR LA LÓGICA DE 'establishOutgoingCall' O 'answer' SI YA FUNCIONAN.
   - PROTOCOLO ANTI-COLISIÓN (BUSY TONE): Si un usuario remoto está transmitiendo (RX activo), el PTT local DEBE bloquearse. Al intentar pulsar, debe sonar el zumbido de "Ocupado" y el botón debe volver a su estado de reposo inmediatamente aunque se mantenga el dedo puesto.
   - LIBERACIÓN SAGRADA DE PTT: El comando de "Apagar Micro" (`active = false`) es INEVITABLE. Prohibido ignorar este comando bajo ninguna circunstancia (ni por Roger Beeps ni por tonos de colisión).
   - SINCRONIZACIÓN DE INTERFAZ: La variable `isTransmitting` en UI debe comprobar siempre que NO haya bloqueo por RX (`!isPttBlockedByRx`) para evitar que el botón se quede "pillado" en rojo visualmente durante una colisión.
   - REGLA DE ORO: Si la voz funciona y está comprobada, ESTÁ SELLADA. Solo se permiten arreglos si hay un fallo total reportado por el usuario.
